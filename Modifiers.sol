pragma solidity >=0.4.24;

contract Modifiers {
    // Intialize a public variable that sets a minimum bidding amount

    // Write a modifier that only accepts bids greater than this minimum amount;
    // otherwise, throw a revert style of excpetion

    // Write a function "bid" that applies this modifier and only lets bids higher 
    // then the minimum amount, execute the code inside this function

    // Bonus: Write code inside the "bid" function
}